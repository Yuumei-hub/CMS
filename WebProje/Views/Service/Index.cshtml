<!-- Slide Indexi-->
@using EntityLayer.Concrete
@model List<Service>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}
<div class="col-lg-12 grid-margin stretch-card">
    <div class="card">
        <div class="card-body">
            <h4 class="card-title">Services</h4>
            <h4 class="card-title">Services</h4>

            <div class="table-responsive">
                <table class="table">
                    <thead>
                        <tr>
                            <th>#</th>
                            <th>Title</th>
                            <th>Description</th>
                            <th>Delete</th>
                            <th>Update</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>@item.ServiceId</td>
                                <td>@item.Title</td>
                                <td>@item.Description</td>
                                <td><a href="/Service/DeleteService/@item.ServiceId" class="btn btn-outline-danger">Delete</a></td>
                                <td><a href="/Service/UpdateService/@item.ServiceId" class="btn btn-outline-success">Update</a></td>
                                <td></td>
                            </tr>
                        }
                    </tbody>
                </table>
                <a href="/Service/CreateService/" class="btn btn-outline-info"> Create New Service</a>
            </div>
        </div>
    </div>
</div>




